# Direct runtime dependencies for wikiSpyder.
# Transitive dependencies are intentionally omitted; pip will resolve them.
aiohttp==3.13.3
beautifulsoup4==4.14.3
Pillow==12.1.1
PyQt6==6.5.0
requests==2.33.0
